{% extends "book/base.html.twig" %}

{% block body %}
	<h2>
		Vous allez modifier le livre à l'id :
		{{id}}
		// Son nom est :
		{{titre}}
	</h2>

	<form method="POST">
		<div>
			<input name="title" type="text" placeholder=" {{titre}} ">
		</div>
		<div>
			<input name="price" type="number" step="0.01" placeholder="{{price}}">
		</div>
		<div>
			<textarea name="description" placeholder="{{desc}}"></textarea>
		</div>
		<div>
			<button type="submit">Sauvegarde le livre</button>
		</div>
	</form>
{% endblock %}
